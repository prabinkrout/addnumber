pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/prabinkrout/addnumber.git'
            }
        }
        stage('Build') {
            steps {
                sh 'hello'
            }
        }
    }
}
